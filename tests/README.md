# QA-автотесты

Отдельный слой качества продукта.

```text
tests/
├── api/    # API-автотесты (pytest + requests), отчёты Allure
├── ui/     # UI-автотесты (Playwright)
└── load/   # нагрузочные (Locust)
```

Стратегия — в `docs/11_Test_Strategy.md`.
